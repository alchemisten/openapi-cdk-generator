{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap && lerna exec --no-bail -- npm run setup-dev",
    "link": "lerna link"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
